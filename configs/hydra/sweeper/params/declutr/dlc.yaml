task/declutr: sem_long
model_checkpoint: base

task.declutr.batch_size: 16
task.declutr.sem_noise: 0.0, 0.05, 0.2

max_steps: 20000

limit_val_batches: 4
val_check_interval: 512
strategy: ddp_find_unused_parameters_true


sweep_id: declutr/dlc_clean

