task/dcse: base
model_checkpoint: base
effective_batch_size: 1024
task.dcse.batch_size: 64
task.dcse.lr_warmup_steps: 1000
task.dcse.lr: 8e-5
task/dcse/dataset: fineweb
task/dcse/encoder/sem: h2
task/dcse/encoder/dino_head: base
task.dcse.encoder.sem.per_simpex_ln: true
task.dcse.delta_margin: 0.5
task.dcse.delta_ent: 1.0
task.dcse.reg_warmup_steps: 12000
task.dcse.sem_reset_threshold: 1e-4
task.dcse.sem_reset_schedule: [5000, 10000, 1000]

task.dcse.sem_reset: true, false
task.dcse.reg_type: ent, margin
task.dcse.ce_loss: true, false

compile: false
max_steps: 15000

limit_val_batches: 4
val_check_interval: 256

sweep_id: dcse/sweep0

