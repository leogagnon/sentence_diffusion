defaults:
  - submitit_slurm

mem_gb: 256
gres: gpu:l40s:1
tasks_per_node: 1
array_parallelism: 32
cpus_per_task: 1
timeout_min: 300
partition: long

setup:
  - "source ~/sentence_diffusion/venv/bin/activate"