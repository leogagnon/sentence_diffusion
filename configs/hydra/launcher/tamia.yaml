defaults:
  - submitit_slurm

mem_gb: 300
gpus_per_node: 4
array_parallelism: 32
nodes: 1 
tasks_per_node: 4
cpus_per_task: 12
timeout_min: 180
account: aip-glaj
additional_parameters:
  no-requeue: true

setup:
  - "source ~/sentence_diffusion/venv/bin/activate"